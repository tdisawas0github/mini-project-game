import Game from './components/Game';
import './App.css';

/**
 * Root application component.
 *
 * Renders the top-level Game component for the app.
 *
 * @returns The App component's JSX element containing the Game.
 */
function App() {
  return <Game />;
}

export default App;
